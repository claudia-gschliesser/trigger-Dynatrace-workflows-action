export enum Inputs {
  clientId = 'client_id',
  clientSecret = 'client_secret',
  tenant = 'tenant',
  endpoint = 'endpoint',
  workflowId = 'workflow_id',
  payload = 'payload',
}

export enum Outputs {
  responseBody = 'response_body',
}
